#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct personne{
 char nom[20];
 char prenom[20];
 int age;
};

int main()
{
    struct personne p1;
    strcpy(p1.nom, "meriem");
    strcpy(p1.prenom,"bsila");
    p1.age=22;
    printf("%s\n",p1.nom);
    printf("%s\n",p1.prenom);
    printf("%d\n",p1.age);
    return 0;
}
