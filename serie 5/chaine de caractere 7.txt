
#include <stdio.h>
#include <string.h>
#include <ctype.h>
int main() {
  char a[10];
  int i;
  printf("entrer la chaine a: ");
  fgets(a,sizeof(a),stdin);

  for(i=0;a[i]!='\0';i++){
     a[i]=toupper(a[i]);
  }
 printf("%s",a);

    return 0;

}


