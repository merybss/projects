
#include <stdio.h>
#include <string.h>

int main() {
  char a[10];
  char b[10];
  printf("entrer la chaine a: ");
  fgets(a,sizeof(a),stdin);
   printf("entrer la chaine b: ");
  fgets(b,sizeof(b),stdin);
  strcat(a,b);

  printf("la chaine combine est %s",a);

    return 0;

}


