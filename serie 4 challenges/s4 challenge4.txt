#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,max,n;
    printf("combien d'element voulez vous?: \n");
    scanf("%d",&n);
    int t[n];
    for(i=0;i<n;i++){
    printf("entrer l'element %d\n" ,i+1);
    scanf("%d",&t[i]);
    }
    for(i=0;i<n;i++){
    if( t[i]>max)
    max=t[i];
    }
    printf("la valeur max est %d\n",max);

    return 0;
}
