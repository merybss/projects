#include <stdio.h>
#include <stdlib.h>

int main()
{
  int i;
int notes[5] = {10, 12, 14, 16, 18};

    for (int i = 0; i < 5; i++) {
        printf("notes[%d] = %d\n", i, notes[i]);
    }

    return 0;
}
