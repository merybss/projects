#include <stdio.h>
#include <stdlib.h>

int main()
{
int c;
    printf("enter un caractere: \n");
    scanf(" %c",&c);
    if(c>=65 && c<=90){
        printf("%c est une lettre alphabetique majuscule \n",c);
    }
     else if(c>=97 && c<=122){
        printf("%c est une lettre alphabetique minuscule \n",c);
    }
    else{
        printf("%c n'est pas une lettre",c);
    }
    return 0;
}
