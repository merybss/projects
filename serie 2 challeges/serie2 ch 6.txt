#include <stdio.h>
#include <stdlib.h>

int main()
{
   int x;
    printf("entrer le nombre entier x: ");
    scanf("%d",&x);
    if(x>0){
        printf("%d est un nombre positif \n",x);
    }else if(x<0){
    printf("%d est un nombre negatif \n",x);
    }
    else{
        printf("%d est null \n",x);
    }
    return 0;
}
